from django.shortcuts import render

# Create your views here.

"""
If we want we may create all Api's view part here.
Currently we have create different-different files for views(we assume it's major part of the project), 
to manage and debug easly.
"""